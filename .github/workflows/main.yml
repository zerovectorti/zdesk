name: Print secret

on:
  workflow_dispatch:



jobs:
  build-deploy:
    name: Print secret
    runs-on: ubuntu-latest
    steps:
    - name: Dump secret
      run: echo -n "${{ secrets.RENDEZVOUS_SERVER }}" | xxd -ps
